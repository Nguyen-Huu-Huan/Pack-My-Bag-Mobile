import React from "react";
import { View, Text, SafeAreaView, ScrollView } from "react-native";
import { Header } from "react-native-elements";

const HomeHeader = () => {
	return <Header centerComponent={{ text: "", style: { color: "#fff" } }} />;
};
export default HomeHeader;
